<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.study.dao.IUserDao">
    <!-- 配置查询所有操作 -->
    <select id="findAll" resultType="com.study.domian.User">
        select * from tb_user
    </select>
    <select id="findById" resultType="com.study.domian.User" parameterType="int">
        SELECT  *FROM  tb_user where id =#{id}
    </select>

    <!--没有配置 type默认推断类型-->
    <insert id="insert" parameterType="com.study.domian.User" >
        <selectKey keyColumn="id" keyProperty="id" resultType="integer">
            SELECT LAST_INSERT_ID();
        </selectKey>
        INSERT INTO tb_user (username,password,brithDate) VALUES(#{username},#{password},#{brithDate})
    </insert>
    <update id="updateById" parameterType="com.study.domian.User">
        UPDATE  tb_user set username=#{username} where id=#{id}
    </update>
    <delete id="deleteById" parameterType="integer">
        DELETE  FROM  tb_user where id=#{id}
    </delete>
    <select id="selectCount" resultType="integer">
        SELECT  COUNT(1) FROM  tb_user;
    </select>
</mapper>